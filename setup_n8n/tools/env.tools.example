#####################################
#      TOOLS STACK - .env.example   #
#####################################

# ========= POSTGRES (usuário administrador para init) =========
POSTGRES_USER=admin
POSTGRES_PASSWORD=adminpass
POSTGRES_DB=n8n

# ========= USUÁRIO DE APLICAÇÃO (N8N) =========
POSTGRES_NON_ROOT_USER=n8n
POSTGRES_NON_ROOT_PASSWORD=n8npass

# ========= REDIS =========
REDIS_PORT=6379

# ========= N8N =========
# Porta onde o N8N será acessível
N8N_PORT=5678

# Protocolo de acesso (pode mudar para https com proxy reverso)
N8N_PROTOCOL=http

# Host usado para acessar o N8N (ex: n8n.local apontando para localhost)
N8N_HOST=n8n.local

# Ativa a autenticação básica (recomenda-se ativar em produção)
N8N_BASIC_AUTH_ACTIVE=false

# Fuso horário do servidor
N8N_TIMEZONE=America/Sao_Paulo
